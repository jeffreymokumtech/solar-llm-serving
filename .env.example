# Copy to .env on the GPU instance. Nothing here is secret except HF_TOKEN.
HF_TOKEN=
ENGINE_BUCKET=            # S3 bucket from terraform output
GATEWAY_API_KEYS=nv-demo:600,nv-bench:100000
GATEWAY_BEDROCK_MODEL_ID= # e.g. qwen.qwen3-next-80b-a3b to enable the fallback
AWS_REGION=eu-west-1
